#pragma once
#include <vector>
#include "iostream"
#include "fstream"

class Trebuchet
{
public:
	/// <summary>
	/// Test that file can be opened then read input into a string vecotr.
	/// </summary>
	/// <param name="input"></param>
	/// <param name="fileName"></param>
	static void readFile(std::vector<std::string> input, std::string fileName);






};

